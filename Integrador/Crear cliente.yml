info:
  name: Crear cliente
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/clientes
  body:
    type: json
    data: |-
      {
        "nombre": "Cliente de prueba"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
